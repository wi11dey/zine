# Haskell style

When generating or editing Haskell code in this repository, always use the `UnicodeSyntax` language extension. Don’t add it to the beginning of files other than Setup.hs; it is already enabled by package.yaml.
